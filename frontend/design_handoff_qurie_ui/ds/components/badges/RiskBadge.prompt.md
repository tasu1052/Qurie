Marks learners returned by `?flag=at-risk`. Always accompanied by the reason ("최근 2세션 미참여 · 정확도 62%") and, where the badge appears in a filtered list, by the criteria caption on the filter chip row. warning = 주의, danger = 위험 (sustained decline). Never used for scores on their own.
```jsx
<RiskBadge level="danger" />
```