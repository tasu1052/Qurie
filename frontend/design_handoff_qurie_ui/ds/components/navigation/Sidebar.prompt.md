App sidebar — icon+label items, indigo active tint, collapsible.
```jsx
<Sidebar activeKey="classes" items={[{key:'dash',label:'대시보드',icon:<HomeIcon/>},{key:'classes',label:'클래스',icon:<BookIcon/>,badge:4}]} logoSrc="assets/logo.png" />
```