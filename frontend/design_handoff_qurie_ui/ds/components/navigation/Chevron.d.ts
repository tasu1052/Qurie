/** The Q>rie brand chevron ">" glyph — breadcrumb separators, expand indicators, next-step arrows. Use sparingly. */
export interface ChevronProps {
  color?: string;
  size?: number;
  style?: React.CSSProperties;
}
export declare function Chevron(props: ChevronProps): JSX.Element;