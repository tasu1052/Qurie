Dropdown selector; size="sm" is designed to sit inline inside a table row (role assignment).
```jsx
<Select size="sm" value="STUDENT" options={['ADMIN','TEMP_ADMIN','STUDENT']} onChange={v=>…} />
```
Options accept strings or {value,label}.