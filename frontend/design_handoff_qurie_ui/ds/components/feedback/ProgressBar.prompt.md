Determinate for known totals (uploads, exports); indeterminate for syncs with no total. Percentages render in mono.
```jsx
<ProgressBar value={62} label="파일 업로드 · repo.zip" />
<ProgressBar label="CRDT 스냅샷 동기화" hint="진행 중" />
```